class HashSet:
    # 增，向哈希集合中添加一个元素，复杂度 O(1)
    def add(self, key: int) -> None:
        pass

    # 删，删除哈希集合中的一个元素，复杂度 O(1)
    # 如果元素不存在，则不做任何事情
    def remove(self, key: int) -> None:
        pass

    # 查，是否存在哈希集合中，复杂度 O(1)
    def contains(self, key: int) -> bool:
        pass